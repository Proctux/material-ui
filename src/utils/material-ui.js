import { createMuiTheme } from "@material-ui/core";

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#740000',
      redLightest: '#74000020',
    },
    custom: {
      gray64: '#787878',
    },
  },
})

export default theme
